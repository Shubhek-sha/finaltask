import type { Meta, StoryObj } from "@storybook/react-vite";
import { {{name.pascal}} } from "./{{name.pascal}}";

const meta: Meta<typeof {{name.pascal}}> = {
  title: "Components/{{name.pascal}}",
  component: {{name.pascal}},
  tags: ["autodocs"],
};

export default meta;
type Story = StoryObj<typeof {{name.pascal}}>;

export const Default: Story = {
  args: {},
};
