/**
 * @forge/types — shared TypeScript types and Zod schemas.
 *
 * This package is the single source of truth for shapes shared between
 * apps/web and apps/api (domain entities, RBAC roles, API contracts,
 * socket event payloads). Populated module-by-module starting in Phase 3
 * (Backend foundation) — see ARCHITECTURE.md §6.
 */

export const FORGE_TYPES_VERSION = "0.0.0";
