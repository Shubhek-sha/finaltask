/**
 * @forge/sdk — typed API client + socket event contracts.
 *
 * Consumed only by apps/web. Wraps REST calls (typed against @forge/types)
 * and the socket.io-client event map (see ARCHITECTURE.md §9). Built out
 * starting Phase 4 (Auth + RBAC) alongside the first real API endpoints.
 */

export const FORGE_SDK_VERSION = "0.0.0";
